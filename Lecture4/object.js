let person = {
    name: 'maaz',
    age: 25
}

// write the object value and change that using dot notation
person.age = 26

// write the object value using bracket notation
person['name'] = 'kashif'

// access all objects values
console.log(person)

// access specific object
console.log(person.name)